allprojects {
    group = 'org.gradle.common'
    version = '1.0'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'maven'

    uploadArchives {
        repositories {
            mavenDeployer {
                repository(url: "file://$rootProject.projectDir/../repo")
            }
        }
    }
}